# 项目状态

## 当前阶段

Vibe Coding 步骤 16：改完必须验证和人工确认。

## 已确认决策

| 项目 | 决策 |
| --- | --- |
| 部署目标 | 公司华润雪花服务器 |
| 前端 | React + Vite + TypeScript |
| 后端 | Python FastAPI |
| 数据库 | SQLite |
| 后台任务 | FastAPI 内置 worker + asyncio 队列 |
| 默认向量模型 | `qwen3-vl-embedding` |
| 默认多模态模型 | `qwen3-vl-plus` |
| 模型配置 | 前端可修改模型名、阈值、Top 候选数量、模型超时时间、图片提示词和临时 Key，后端可保存默认 Key |
| 部署方式 | nginx + systemd |

## 已完成

- 需求倒排确认。
- PRD 和验收标准确认。
- 页面结构和视觉方向确认。
- 项目边界和非功能需求确认。
- 技术栈确认。
- 架构草稿确认。
- 三份同步文档已创建：`docs/PRD.md`、`docs/ARCHITECTURE.md`、`docs/STATUS.md`。
- 人工验收清单已创建：`docs/ACCEPTANCE_CHECKLIST.md`。
- 开发规范已创建：`docs/DEVELOPMENT.md`。
- 参考资料目录已创建：`references/README.md`。
- 环境变量示例已创建：`backend/.env.example`。
- 根目录 `.gitignore` 已创建，排除环境变量、上传文件、生成索引和依赖目录。
- Git 仓库已初始化。
- 当前阶段质量门禁已创建：`scripts/quality_check.py`。
- 质量门禁说明已创建：`docs/QUALITY.md`。
- 后端最小 FastAPI 骨架已创建：`backend/app/main.py`。
- 前端最小 React/Vite 工作台骨架已创建：`frontend/src/App.tsx`。
- `/api/health` 和 `/api/status` 已可访问。
- 前端开发服务已可通过 `http://127.0.0.1:8080` 访问。
- 单张图片识别 API 已创建：`POST /api/recognize`。
- 前端已支持单张图片选择、预览、上传识别和 Top 5 候选展示。
- 模板图片开发代理已修复：`/template-images` 由 Vite 代理到后端。
- 模型运行时配置接口已创建：`GET/PUT/POST /api/settings/models`。
- 前端模型设置面板已支持修改模型名、阈值和复核开关。
- 模板库上传接口已创建：`POST /api/templates/upload`。
- 前端已支持上传一个或多个 `.xlsx` 模板库文件并触发抽取和索引重建。
- 模板源文件列表接口已创建：`GET /api/templates/sources`。
- 模板源文件删除接口已创建：`DELETE /api/templates/sources/{filename}`，删除后自动用剩余文件重建模板库。
- 前端已显示每个已构建模板源文件，并支持单个删除。
- 图片链接识别接口已创建：`POST /api/recognize-url`。
- 前端已支持粘贴图片链接并识别。
- 业务员页面已新增“表格批量上传链接”，支持上传 `.xlsx` 并读取“照片链接”字段生成批量链接识别队列。
- 前端业务员页面已支持选择文件夹，自动过滤图片并按顺序批量识别。
- 批量识别结果已拆分展示产品编码、产品名称、瓶/听/箱，并支持导出 Excel。
- 批量识别结果已支持单条重试和批量重试失败/未命中记录。
- 业务员批量识别结果已支持展示原图和命中模板图，并可人工标记正确/错误后统计已审核正确率。
- 业务员批量识别结果已新增图片处理序号，文件夹和表格链接批量识别都会按处理顺序显示 `#1、#2、#3...`。
- 识别链路已新增唯一识别编号 `traceId`：批量任务按 `ByyyyMMddHHmmss-随机码-001` 生成，业务员页面、管理员日志和导出 Excel 都展示同一个编号；单条/批量重试沿用原编号，方便追踪同一张图的多次识别。
- 分层识别 v1 已接入：Qwen 视觉理解、包装类型过滤、关键词召回、视觉分重排和融合分。
- 前端已展示识别依据：来源、先判类型、多产品、品牌、关键词、可见文字、人工提示。
- 前端已支持辅助关键词输入，用于未配置 Key 时验证关键词召回链路。
- 识别接口已支持请求头 `X-DashScope-Api-Key` 覆盖后端默认 Key。
- 前端模型设置已支持“前端临时 Key”：填写后本浏览器识别请求优先使用，不填则使用后端默认 Key。
- 后端模型设置已支持保存默认 DashScope Key 到运行时配置文件 `backend/data/model_settings.json`，该文件不会提交到 Git。
- 识别依据已显示 Key 来源：前端临时 Key、后端保存 Key、后端环境变量或未使用 Key。
- 三层筛选已收紧为严格流程：先按瓶/听/箱过滤，再按品牌/系列关键词强筛，最后在候选内按图片语义相似度排序。
- 模板匹配已升级为结构化标签索引：产品名称会提取品牌/系列、容量、度数、包装规格、瓶色/提手/纸箱等标签；上传图识别文字也会用同一套规则扩展检索标签。
- 识别链路已增加可配置的雪花体系品牌门禁：如果上传图片识别结果中没有提取到管理员配置的雪花体系品牌/系列，直接返回未命中，不进入模板匹配。
- 管理员模型设置已新增“雪花品牌配置”，支持维护雪花、纯生、勇闯天涯、SuperX、红爵、喜力、虎牌、乌苏、AMSTEL 等品牌清单。
- 品牌门禁已过滤所有 `*-生动化检查-*` 巡检水印、手机时间、服务器时间、门店地址等非商品文字；如果同时出现雪花体系品牌和竞品品牌，会继续匹配但标记人工审核，只有未提取到雪花体系品牌时才直接未命中。
- 识别前已增加底部巡检文字裁剪：检测到白底红字的 CRM 图片底部信息时，会先裁掉该区域，再送给 Qwen 识别和模板相似度匹配；裁剪检测已改为先识别底部连续白色信息区，再确认其中红字，日志会显示是否触发“底部文字裁剪”。
- 识别链路已增加弱商品证据门禁：如果裁剪后只识别到边缘、角落、远处、模糊或“疑似”的小纸箱/小商品，没有容量规格等强商品证据，直接未命中，避免模型脑补品牌。
- 品牌门禁已升级为可解释结果：日志会显示“门禁命中品牌”和“竞品命中品牌”；管理员配置中如果误混入 `U`、`8`、`U8` 等短碎片词，会被门禁过滤，不再让燕京 U8 这类竞品图通过雪花品牌门禁。
- 品牌/关键词筛选已过滤单字母和纯数字碎片，避免 `U`、`8` 这类词把关键词分、品牌分异常拉高。
- 模板匹配已增加“命中品牌强约束”：图片门禁命中 `红爵 / AMSTEL` 时，二层筛选只允许红爵/AMSTEL 模板进入候选；`经典、500ml、绿瓶、12瓶` 等关键词只能在同品牌模板内排序，不能跨品牌命中雪花经典等模板。
- 管理员页面已新增“识别日志”，可查看每张图片的门禁判断、清洗后文字、三层筛选过程、Top 候选和最终模板命中原因，并支持刷新、清空、序号和分页；默认每页 50 条。
- 管理员识别日志已新增后端分页筛选和查找：支持按识别编号、文件、产品、品牌、清洗文字等关键词搜索，并可按命中状态、品牌门禁、竞品/混合图筛选。
- 开放接口第一版已新增：`POST /api/open/recognitions` 支持业务系统通过图片链接调用单图识别，使用 `Authorization: Bearer <token>` 鉴权，返回产品编码、产品名称、瓶/听/箱、相似度、模板图、门禁品牌和原始结果；`GET /api/open/status` 可查看开放接口密钥是否配置。
- 管理员页面已新增“开放接口”面板，可查看 Token 脱敏状态，并生成/轮换 `Authorization: Bearer <OPEN_API_TOKEN>`；新 Token 仅生成后本次明文显示。
- 开放接口文档已新增：`docs/OPEN_API.md`，包含鉴权、单图识别请求体、返回体和状态检查说明。
- 图片链接识别已增加稳定性兜底：URL 下载遇到 429/500/502/503/504、网络错误或超时时会自动重试一次；Qwen 视觉模型返回错误/超时会自动重试一次，仍失败则走本地视觉相似度兜底并写入识别日志。
- 开放接口已新增批量任务第一版：`POST /api/open/batch-jobs` 创建后台图片链接识别任务，`GET /api/open/batch-jobs/{job_id}` 查询进度，`GET /api/open/batch-jobs/{job_id}/items` 分页查询处理结果；结果使用本地 JSONL 落盘，后台线程顺序处理，服务重启会将未完成任务标记为中断。
- 开放接口已新增 Excel 批量上传任务：`POST /api/open/batch-jobs/workbook` 支持上传包含“照片链接”字段的 `.xlsx`，自动读取链接并创建后台任务，`trace_id` 自动使用 `Sheet1-第2行` 格式。
- 管理员模型设置已支持统一配置默认任务模型策略和多个模型策略；业务员页面不展示模型选择，默认按管理员配置执行。开放接口仍支持 `model_profile` 按任务覆盖：`default` 使用管理员默认策略，传具体模型名可指定视觉模型，`主模型|备用模型` 支持模型回退，`high_accuracy` 或 `high_accuracy:模型A,模型B,模型C` 支持多模型复核并融合品牌/包装、OCR 文字和最终判断结果；管理员日志和结果会记录每次模型调用的角色、模型名、耗时、状态和输出摘要。
- 管理员模型设置已支持多平台模型：`aliyun:模型名` 走阿里 DashScope，`volc:模型名/接入点ID` 走火山 Ark；模型不写平台前缀时使用管理员配置的默认模型平台。模型调用日志会记录 provider、model、耗时和输出摘要。
- 服务器现有 731 张模板图索引已补写结构化标签，无需重新抽取 Excel。
- 前端已新增登录页：管理员账号 `admin/admin`，业务员账号 `root/2345`。
- 管理员页面包含：模型设置、模板库、Top 候选数量设置、图片提示词。
- 管理员图片提示词已内置雪花品牌专用默认提示词；用户留空时后端自动使用该默认提示词。
- 管理员页面已支持设置 Qwen 模型超时时间，默认 10 秒；超时后自动回退本地视觉匹配。
- 业务员页面只保留上传识别相关功能和识别结果。
- Excel 模板抽取脚本已存在：`tools/extract_excel_templates.py`。
- 本地基线图片匹配脚本已存在：`tools/match_uploaded_image.py`。
- 当前 Excel 已抽取过 112 个产品记录和 731 张模板图片。
- 公司服务器已部署到 `http://8.160.163.200:18086`，systemd 服务 `cr-snow-image-recognition.service` 正常运行。

## 待完成

| 步骤 | 内容 | 状态 |
| --- | --- | --- |
| 8 | 开发规范和参考资料夹 | 已完成 |
| 9 | Git 和质量门禁 | 已完成 |
| 10 | 可运行最小骨架 | 已完成 |
| 11 | AI 协作协议 | 已确认 |
| 12 | 第一个端到端切片 | 已完成 |
| 13 | 模块拆分和边界维护 | 已完成 |
| 14 | 安全底线检查 | 已完成 |
| 15 | 科学应对报错 | 已完成 |
| 16 | 改完必须验证和人工确认 | 待人工确认 |
| 19 | 部署上线 | 已部署到公司服务器 |

## 风险

- 已在聊天中出现过阿里模型 Key，上线前建议轮换。
- `qwen3-vl-embedding` 的实际 API 入参和返回结构需要在接入时验证。
- 文件夹批量上传可能受到浏览器和 nginx 上传大小限制，需要部署时配置。
- 第一版单机 worker 适合中小规模任务，大规模并发时需要升级任务队列。
